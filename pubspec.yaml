name: kasir_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  # 🔥 Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2

  # 📱 Fitur tambahan
  mobile_scanner: ^4.0.1
  simple_barcode_scanner: ^0.3.0
  google_sign_in: ^6.2.1
  http: ^1.2.2
  image_picker: ^1.1.2
  pdf: ^3.8.4
  path_provider: ^2.1.4
  flutter_local_notifications: ^17.2.3
  esc_pos_bluetooth: ^0.4.1
  esc_pos_utils: ^1.1.0
  share_plus: ^10.0.2
  file_selector: ^1.0.3
  shared_preferences: ^2.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.11.0

dependency_overrides:
  flutter_bluetooth_basic:
    path: local_plugins/flutter_bluetooth_basic

flutter_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "image/Nira POS.png"
  web:
    generate: true
    image_path: "image/Nira POS.png"
  windows:
    generate: true
    image_path: "image/Nira POS.png"
  macos:
    generate: true
    image_path: "image/Nira POS.png"
  linux:
    generate: true
    image_path: "image/Nira POS.png"

flutter:
  uses-material-design: true
  assets:
    - image/Nira POS.png
    - image/nira_posbaru.png
